<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.repository.UserMapper">
    <resultMap id="UserResult" type="com.lab.entity.User">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findById" resultMap="UserResult">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResult">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserResult">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findByStudentId" resultMap="UserResult">
        SELECT * FROM users WHERE student_id = #{studentId}
    </select>

    <insert id="insert" parameterType="com.lab.entity.User">
        INSERT INTO users (student_id, username, email, password, role, status)
        VALUES (#{studentId}, #{username}, #{email}, #{password}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.lab.entity.User">
        UPDATE users
        SET username = #{username}, email = #{email}, password = #{password}, role = #{role}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>